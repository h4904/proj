services:
  - type: web
    name: proj
    env: python
    plan: free
    buildCommand: ""
    startCommand: "python server.py"
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: true
